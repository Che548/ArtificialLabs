'use client';
import {useState,type FormEvent} from 'react';
import {LockKeyhole,ArrowRight,ScanLine} from 'lucide-react';
import Modal from './Modal';
import {api} from '../lib/client';
export default function Login(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);setBusy(true);setError('');try{await api('/api/auth','POST',{login:form.get('login'),password:form.get('password')});location.replace('/');}catch(e){setError((e as Error).message);setBusy(false);}}return <><div className="locked-shell" aria-hidden="true"><div className="brand">сфера <span>scan</span></div><div className="locked-preview"><ScanLine size={72} strokeWidth={1}/><h1>Точность в деталях.</h1><p>Сканирование тест-полоски в несколько шагов.</p></div></div><Modal title="Вход в Сфера Scan" hideTitle><div className="login-symbol"><LockKeyhole size={26} strokeWidth={1.5}/></div><form onSubmit={submit} className="form-stack"><label>Логин<input name="login" autoComplete="username" autoCapitalize="none" required autoFocus maxLength={128}/></label><label>Пароль<input name="password" type="password" autoComplete="current-password" required maxLength={256}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="primary" disabled={busy}>{busy?'Входим…':'Продолжить'}{!busy&&<ArrowRight size={18}/>}</button></form><p className="caption center">Есть ссылка-приглашение? Откройте её для входа.</p></Modal></>;}
