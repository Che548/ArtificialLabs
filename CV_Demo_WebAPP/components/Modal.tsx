'use client';
import { useEffect,useId,useRef,type ReactNode } from 'react';
import { X } from 'lucide-react';
export default function Modal({title,children,onClose,wide=false,hideTitle=false}:{title:string;children:ReactNode;onClose?:()=>void;wide?:boolean;hideTitle?:boolean}){const ref=useRef<HTMLDialogElement>(null);const id=useId();useEffect(()=>{const dialog=ref.current!;dialog.showModal();const previous=document.body.style.overflow;document.body.style.overflow='hidden';return()=>{dialog.close();document.body.style.overflow=previous;};},[]);return <dialog className={`modal ${wide?'wide':''}`} ref={ref} aria-labelledby={id} onCancel={e=>{e.preventDefault();onClose?.();}} onClick={e=>{if(e.target===ref.current&&onClose){const r=ref.current.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)onClose();}}}><div className={hideTitle?"visually-hidden":"modal-heading"}><h2 id={id}>{title}</h2>{onClose&&<button className="icon-button" aria-label="Закрыть" onClick={onClose}><X size={20}/></button>}</div>{children}</dialog>;}
