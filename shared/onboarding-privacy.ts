/** Initial product settings; later permission changes remain under user control. */
export const LOCAL_ONBOARDING_PRIVACY = Object.freeze({
  cloudSyncEnabled: true,
  anonymousAnalytics: true,
  medicalRecommendations: true,
});
