import { View } from 'react-native';
export { AppText, SymbolView } from '../legal-ui/stubs';
export const ProfileSettingsGroup = ({ children }: { children: React.ReactNode }) => <View>{children}</View>;
