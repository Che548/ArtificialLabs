appId: ${MAESTRO_APP_ID || "com.anonymous.privateexpo"}
---
# Keep all iOS application flows in one XCUITest session. On iOS 26 the
# Maestro localhost driver can become unavailable between separate CLI runs.
- runFlow: open-dev-client.yml
- runFlow: ios-primary.yml
- runFlow: product-surface.yml
